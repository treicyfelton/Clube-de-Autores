= form_for(@rule) do |f|
  - if @rule.errors.any?
    #error_explanation
      %h2
        = pluralize(@rule.errors.count, "error")
        prohibited this rule from being saved:
      %ul
        - @rule.errors.full_messages.each do |msg|
          %li= msg
  = f.text_area :uprules, cols:"80", class:'rulesbox'
  .actions
    = f.submit class: 'submit', value: 'Atualizar'